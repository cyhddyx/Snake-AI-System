# 复制本文件为 .env 并填入真实值。
# .env 已被 .gitignore 排除，切勿把真实密钥提交到仓库。

# 数据库连接（必填，缺失时后端无法启动）
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/snake_db

# DeepSeek 智能问答（/chat 接口使用）
DEEPSEEK_API_KEY=your_deepseek_api_key

# 备用大模型服务密钥（可选）
SILICON_FLOW_API_KEY=your_siliconflow_api_key

# Token 签发密钥（生产环境必须替换为强随机值）
AUTH_SECRET_KEY=replace_with_a_strong_secret

SQL_ECHO=false
ACCESS_TOKEN_EXPIRE_MINUTES=10080
CORS_ORIGINS=http://127.0.0.1:5173,http://localhost:5173
